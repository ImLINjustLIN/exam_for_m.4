/* ============================================================
   content.js — เนื้อหาสรุปตั้งต้นของแต่ละหัวข้อ
   ------------------------------------------------------------
   ไฟล์นี้เติมเข้าไปใน STARTER_CONTENT (โน้ตปกติ)
   และ STARTER_DIAGRAM (โน้ตไดอะแกรม) ที่ประกาศไว้ใน config.js
   นักเรียนแก้/ลบ/เพิ่มทับได้ตามใจ ของแต่ละคนแยกกันคนละชุด
   ============================================================ */
Object.assign(STARTER_CONTENT, {});
Object.assign(STARTER_DIAGRAM, {});
